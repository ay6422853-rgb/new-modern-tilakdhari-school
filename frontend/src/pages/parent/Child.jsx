import AppLayout from '../../layouts/AppLayout';import CrudPage from '../../components/CrudPage';export default function Page(){return <AppLayout role="parent"><CrudPage title="My Child" resource="students" columns={[{key:'name',label:'Name'},{key:'className',label:'Class'},{key:'admissionNo',label:'Admission'},{key:'status',label:'Status'}]} fields={[]} canEdit={false}/></AppLayout>}
