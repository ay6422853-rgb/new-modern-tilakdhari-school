import {useEffect,useState} from 'react';import AppLayout from '../../layouts/AppLayout';import {dashboard} from '../../api';
export default function Dashboard(){const [d,setD]=useState({});useEffect(()=>{dashboard().then(setD).catch(()=>{})},[]);return <AppLayout role="parent"><h2>Parent Dashboard</h2><p className="muted">Live overview from MongoDB backend.</p><div className="stats"><div className="card statcard">Active Students<div className="num">{d.students??'—'}</div></div><div className="card statcard">Collections<div className="num">₹{Number(d.payments||0).toLocaleString()}</div></div><div className="card statcard">Expenses<div className="num">₹{Number(d.expenses||0).toLocaleString()}</div></div><div className="card statcard">Present Today<div className="num">{d.todayPresent??'—'}</div></div></div></AppLayout>}
